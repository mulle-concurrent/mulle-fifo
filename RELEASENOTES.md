### 0.1.6








* Public symbols are now exported when mulle-fifo is built as part of mulle-core (recognize `MULLE__CORE_BUILD),` preventing missing-symbol or visibility issues for downstream consumers.
* Minor comment cleanup in reflection include to fix wording.
